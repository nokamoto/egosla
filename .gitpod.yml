image:
  file: .gitpod.Dockerfile

tasks:
- init: mysql < deployments/init.sql
  command: make watcher

vscode:
  extensions:
    - zxh404.vscode-proto3
    - golang.go

github:
  prebuilds:
    addBadge: true
