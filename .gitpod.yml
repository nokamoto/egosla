image:
  file: .gitpod.Dockerfile

tasks:
- init: mysqladmin --wait --count 3 ping && mysql < deployments/init.sql
  command: make watcher
- command: getenvoy run standard:1.18.2 -- --config-path deployments/envoy.yaml 
- init: cd web && yarn

vscode:
  extensions:
    - zxh404.vscode-proto3
    - golang.go

github:
  prebuilds:
    addBadge: true

ports:
- port: 8080
- port: 3000
